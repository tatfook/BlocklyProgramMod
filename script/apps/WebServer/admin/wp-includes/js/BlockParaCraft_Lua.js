Blockly.Lua['ParaCraft_Start'] = function(block) {
    // TODO: Assemble Lua into code variable.
    return 'ParaCraft_Start;';
  };
  Blockly.Lua['ParaCraft_FarwardStep'] = function(block) {
    // TODO: Assemble Lua into code variable.
    var code = 'API.step();\n';
    return code;
  };
  Blockly.Lua['ParaCraft_TurnLeft'] = function(block) {
    // TODO: Assemble Lua into code variable.
    var code = 'API.turnLeft();\n';
    return code;
  };
  Blockly.Lua['ParaCraft_TurnRight'] = function(block) {
    // TODO: Assemble Lua into code variable.
    var code = 'API.turnRight();\n';
    return code;
  };
  Blockly.Lua['ParaCraft_DestroyBlock'] = function(block) {
    // TODO: Assemble Lua into code variable.
    var code = 'API.destroyBlock();\n';
    return code;
  };
